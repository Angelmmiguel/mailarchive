<!-- The pane beside the list when no thread is open. -->
<p class="pick">Select a thread to read it. Use ↑ and ↓ to move through the list.</p>

<style>
	.pick {
		margin: auto;
		padding: var(--space-6);
		font: var(--body-md) / var(--body-leading) var(--font-body);
		color: var(--text-faint);
		text-align: center;
	}
</style>
